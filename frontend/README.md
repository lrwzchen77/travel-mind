# Travel Mind Frontend

Vue 3 + Vite application shell for Phase 1.

```bash
npm install
npm test
npm run dev
npm run build
```

Configuration lives in `.env.example`. The default API base URL is `http://localhost:18080/api`.
