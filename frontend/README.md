# Travel Mind Frontend

Vue 3 + Vite application shell for Phase 1.

```bash
npm ci
npm test
npm run dev
npm run build
```

Configuration lives in `.env.example`. The default API base URL is the same-origin `/api`; Vite proxies it to the local backend in development.
